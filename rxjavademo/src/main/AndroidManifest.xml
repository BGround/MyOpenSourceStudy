<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.rxjavademo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <!-- RxJava 介绍主要MainActivity
        <activity android:name=".MainActivity"> -->

        <!--  创建操作符主要使用的activity
        <activity android:name=".operator.CreateOperator">-->

        <!--  转换操作符主要使用的activity
        <activity android:name=".operator.SwitchOperator"> -->

        <!--  组合操作符主要使用的activity
        <activity android:name=".operator.CombineOperator"> -->

        <!--  组合操作符demo
         1、CombineOperatorDemo1 ：从磁盘 / 内存缓存中 获取缓存数据
         2、
         3、-->
        <activity android:name=".demo.CombineOperatorDemo1">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>